#!/bin/bash

set -e

echo "🔐 Running pre-commit checks..."

# 1. git-secret: Automatic encryption of secret files
if command -v git-secret &> /dev/null; then
  echo "🔒 Encrypting secrets with git-secret..."

  # Check for changes in secret files
  changed_secrets=$(git secret list 2>/dev/null | xargs -I {} sh -c 'git diff --cached --name-only | grep -x {} || true')

  if [ -n "$changed_secrets" ]; then
    echo "  ↳ Detected changes in secret files: $changed_secrets"
    git secret hide -m

    # Automatically stage encrypted files
    for file in $(git secret list 2>/dev/null); do
      if [ -f "${file}.secret" ]; then
        git add "${file}.secret"
        echo "  ✓ Added ${file}.secret to staging"
      fi
    done
  else
    echo "  ✓ No secret files changed"
  fi
else
  echo "  ⚠️  git-secret not found, skipping encryption"
fi

# 2. Tests (if test script exists)
# pretest hook automatically runs Biome checks
echo "🧪 Running tests (with pretest checks)..."
if ! npm run test --if-present; then
  echo "❌ Tests failed. Please fix the tests and try again."
  exit 1
fi

# 3. Build verification (if build script exists)
# prebuild hook automatically runs Biome checks and TypeScript type check
echo "🏗️  Running build verification (with prebuild checks)..."
if ! npm run build --if-present; then
  echo "❌ Build failed. Please fix the errors and try again."
  exit 1
fi

echo "✅ All pre-commit checks passed!"
